import React,{useEffect,useState}from"react";
import{createRoot}from"react-dom/client";
import{createClient}from"@supabase/supabase-js";
import{BadgeCheck,BarChart3,Calendar,ChevronRight,ClipboardList,CreditCard,DollarSign,Edit3,FileText,HeartPulse,Home,LogOut,Mail,Megaphone,Package,PlayCircle,Plus,Printer,Search,Share2,ShieldCheck,Trash2,Trophy,Users,Wheat,X}from"lucide-react";
import"./styles.css";

const supabase=createClient(import.meta.env.VITE_SUPABASE_URL,import.meta.env.VITE_SUPABASE_ANON_KEY);
const GST_RATE=.1;
const photos=["/login-photos/photo-1.jpg","/login-photos/photo-2.jpg","/login-photos/photo-3.jpg","/login-photos/photo-4.jpg"];
const WORK_SECTORS=["Jog - Cart","Jog- Lead","Jog- Jogger Machine","Jog/Canter","Gallop","Trot","Hopple","Walking Machine","Water Walk","Race","Trial","Educational Trial","Swim","Beach Work","Recovery"];
const WARMUP_SECTORS=["Jog/Canter","Gallop","Trot","Hopple"];
const isStableRole=r=>["admin","trainer","staff"].includes(r);
const canFinance=r=>["admin","trainer"].includes(r);

const modules={
horses:{table:"horses",title:"Horses",icon:BadgeCheck,profile:true,fields:[["name","Registered Name","text"],["stable_name","Stable Name","text"],["profile_photo_url","Profile Photo URL","text"],["age","Age","number"],["sex","Sex","select",["Gelding","Mare","Horse","Colt","Filly"]],["trainer","Trainer","text"],["status","Status","select",["Racing","Building","Trialling","Spelling","Rehab","Sold","Retired"]],["current_status","Current Status","text"],["next_target","Next Target","text"],["notes","Notes","textarea"]],display:["stable_name","age","sex","trainer","status","current_status","next_target"]},
owners:{table:"owners",title:"Owners",icon:Users,ownerProfile:true,fields:[["name","Owner Name","text"],["email","Email","email"],["phone","Phone","text"],["role","Role","select",["owner","staff","trainer","admin"]],["notes","Notes","textarea"]],display:["email","phone","role","notes"]},
work:{table:"work_entries",title:"Work",icon:ClipboardList,calendarField:"date",fields:[["date","Date","date"],["horse_name","Horse","horseName"],["sector","Work Sector","select",WORK_SECTORS],["warmup","Warm Up Options / Details","conditionalWarmup"],["distance","Distance","text"],["overall_time","Overall Time","text"],["mile_rate","Mile Rate","text"],["last_half","Last Half","text"],["last_quarter","Last Quarter","text"],["driver","Driver","text"],["recovery","Recovery/Heart Rate","text"],["notes","Notes","textarea"]],display:["date","horse_name","sector","warmup","distance","overall_time","mile_rate","last_half","last_quarter","driver","recovery"]},
racing:{table:"race_records",title:"Racing",icon:Trophy,calendarField:"date",fields:[["date","Date","date"],["horse_name","Horse","horseName"],["track","Track","text"],["race","Race/Class","text"],["race_time","Race Time","text"],["barrier","Barrier","text"],["distance","Distance","text"],["status","Status","select",["Nominated","Accepted","Target","Scratched","Completed"]],["driver","Driver","text"],["result","Result","text"],["prizemoney","Prizemoney","number"],["replay_url","Replay URL","text"],["comments","Comments","textarea"],["notes","Notes","textarea"]],display:["date","horse_name","track","race","race_time","barrier","status","driver","result","prizemoney"]},
treatments:{table:"treatments",title:"Vet",icon:HeartPulse,calendarField:"treatment_date",fields:[["treatment_date","Date","date"],["horse_name","Horse","horseName"],["treatment_type","Treatment","text"],["veterinarian","Vet","text"],["bill_amount","Bill Amount","number"],["bill_to_owners","Add to owner invoices?","select",["No","Yes"]],["follow_up_date","Follow Up","date"],["notes","Notes","textarea"]],display:["treatment_date","horse_name","treatment_type","veterinarian","bill_amount","bill_to_owners","follow_up_date"]},
farrier:{table:"farrier_records",title:"Farrier",icon:Package,calendarField:"farrier_date",fields:[["farrier_date","Date","date"],["horse_name","Horse","horseName"],["farrier_name","Farrier","text"],["service_type","Service","select",["Full Set","Fronts","Hinds","Trim","Reset","Bar Shoes","Pads","Glue Ons","Other"]],["shoeing_notes","Shoeing Notes","textarea"],["bill_amount","Bill Amount","number"],["bill_to_owners","Add to owner invoices?","select",["No","Yes"]],["next_due_date","Next Due Date","date"],["notes","Notes","textarea"]],display:["farrier_date","horse_name","farrier_name","service_type","bill_amount","bill_to_owners","next_due_date"]},
feed:{table:"feed_programs",title:"Feed",icon:Wheat,fields:[["horse_name","Horse","horseName"],["morning_feed","Morning Feed","textarea"],["lunch_feed","Lunch Feed","textarea"],["night_feed","Night Feed","textarea"],["supplements","Supplements","textarea"],["notes","Notes","textarea"]],display:["horse_name","morning_feed","lunch_feed","night_feed","supplements"]},
gear:{table:"gear_items",title:"Gear",icon:Package,fields:[["item_name","Item","text"],["horse_name","Horse","horseName"],["category","Category","select",["Hopples","Bridle","Bit","Boots","Harness","Sulky","Driving Gear","Rug","Other"]],["size","Size","text"],["colour","Colour","text"],["condition","Condition","select",["Excellent","Good","Fair","Needs Repair","Retired"]],["location","Location","text"],["assigned_to","Assigned To","text"],["notes","Notes","textarea"]],display:["horse_name","category","size","colour","condition","location","assigned_to"]},
finance:{table:"finance_entries",title:"Finance",icon:DollarSign,calendarField:"entry_date",fields:[["entry_date","Date","date"],["horse_name","Horse","horseName"],["entry_type","Type","select",["Income","Expense"]],["category","Category","text"],["amount","Amount","number"],["paid","Paid","select",["Yes","No"]],["bill_to_owners","Add expense to owner invoices?","select",["No","Yes"]],["notes","Notes","textarea"]],display:["entry_date","horse_name","entry_type","category","amount","paid","bill_to_owners"]},
inventory:{table:"inventory",title:"Inventory",icon:Package,fields:[["item_name","Item","text"],["category","Category","text"],["quantity","Quantity","number"],["supplier","Supplier","text"],["reorder_level","Reorder Level","number"],["notes","Notes","textarea"]],display:["category","quantity","supplier","reorder_level"]},
staff:{table:"staff",title:"Staff",icon:ShieldCheck,fields:[["full_name","Name","text"],["role","Role","text"],["phone","Phone","text"],["email","Email","email"],["notes","Notes","textarea"]],display:["role","phone","email","notes"]}
};

function App(){const[session,setSession]=useState(null),[profile,setProfile]=useState(null),[stable,setStable]=useState(null),[entry,setEntry]=useState("landing"),[tab,setTab]=useState("dashboard"),[loading,setLoading]=useState(true),[toast,setToast]=useState("");
useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session||null);setLoading(false)});const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);if(s)setEntry("app")});return()=>l.subscription.unsubscribe()},[]);
useEffect(()=>{if(session?.user)loadProfile()},[session?.user?.id]);useEffect(()=>{if(!toast)return;const t=setTimeout(()=>setToast(""),3200);return()=>clearTimeout(t)},[toast]);
async function loadProfile(){const{data,error}=await supabase.from("profiles").select("*,stables(*)").eq("id",session.user.id).single();if(error){setToast("Login works, but this account is not linked to a profile.");return}setProfile(data);setStable(data.stables);setTab(data.role==="owner"?"ownerHome":"dashboard")}
if(loading)return <div className="center">Loading...</div>;
if(!session&&entry==="stableLogin")return <Login mode="stable" onBack={()=>setEntry("landing")}/>;
if(!session&&entry==="ownerLogin")return <Login mode="owner" onBack={()=>setEntry("landing")}/>;
if(!session&&entry==="invite")return <InviteSignup onBack={()=>setEntry("landing")} setToast={setToast}/>;
if(!session)return <Landing setEntry={setEntry}/>;
if(!profile)return <main className="page"><div className="card"><h2>Profile not linked</h2><p>Check Supabase profiles for this user.</p><button className="primary" onClick={()=>supabase.auth.signOut()}>Logout</button></div></main>;
return <div className="app">{toast&&<div className="toast">{toast}</div>}<header className="topbar"><div><p>{profile.role==="owner"?"Owners Portal":"Stable App"}</p><h1>{profile.role==="owner"?profile.owner_name||profile.full_name:stable?.name||"Stable"}</h1></div><button className="ghost" onClick={()=>supabase.auth.signOut()}><LogOut size={18}/>Logout</button></header>
{profile.role==="owner"?<OwnerApp profile={profile} tab={tab} setTab={setTab}/>:<StableApp profile={profile} tab={tab} setTab={setTab} setToast={setToast}/>}</div>}
function Landing({setEntry}){return <main className="landing"><PhotoReel/><header className="landing-nav"><strong>The Trotting Stable App</strong><div><button className="ghost dark" onClick={()=>setEntry("stableLogin")}>Stable Login</button><button className="ghost dark" onClick={()=>setEntry("ownerLogin")}>Owners Portal</button><button className="primary light" onClick={()=>setEntry("invite")}>Join With Invite Code</button></div></header><section className="hero"><p className="eyebrow">Harness racing stable software</p><h1>Stable operations and owner communication in one platform.</h1><p>Separate access points for trainers/staff and owners. One database. Role-based permissions.</p><div className="hero-actions"><button className="primary light" onClick={()=>setEntry("stableLogin")}>Stable Login</button><button className="ghost dark" onClick={()=>setEntry("ownerLogin")}>Owners Portal</button></div></section><section className="feature-grid">{["Stable Operations","Owners Portal","Phone Calendar","Invoices","Analytics","Media Updates"].map(x=><article className="feature-card" key={x}><h3>{x}</h3><p>Built into the final rebuild.</p></article>)}</section></main>}
function StableApp({profile,tab,setTab,setToast}){const hidden=profile.role==="staff"?["finance","staff"]:[];return <><nav className="nav"><NavButton active={tab==="dashboard"} onClick={()=>setTab("dashboard")} icon={Home} label="Home"/>{Object.entries(modules).filter(([k])=>!hidden.includes(k)).map(([k,m])=><NavButton key={k} active={tab===k} onClick={()=>setTab(k)} icon={m.icon} label={m.title}/>) }<NavButton active={tab==="updates"} onClick={()=>setTab("updates")} icon={Megaphone} label="Updates"/><NavButton active={tab==="analytics"} onClick={()=>setTab("analytics")} icon={BarChart3} label="Analytics"/>{canFinance(profile.role)&&<NavButton active={tab==="invoices"} onClick={()=>setTab("invoices")} icon={FileText} label="Invoices"/>}</nav>{tab==="dashboard"&&<Dashboard stableId={profile.stable_id} setTab={setTab}/>} {tab==="updates"&&<UpdatesPanel stableId={profile.stable_id} setToast={setToast}/>} {tab==="analytics"&&<Analytics stableId={profile.stable_id}/>} {tab==="invoices"&&canFinance(profile.role)&&<Invoices stableId={profile.stable_id} setToast={setToast}/>} {modules[tab]&&<GenericTable stableId={profile.stable_id} config={modules[tab]} setToast={setToast}/>}</>}
function OwnerApp({profile,tab,setTab}){return <><nav className="nav"><NavButton active={tab==="ownerHome"} onClick={()=>setTab("ownerHome")} icon={Home} label="Home"/><NavButton active={tab==="ownerHorses"} onClick={()=>setTab("ownerHorses")} icon={BadgeCheck} label="Horses"/><NavButton active={tab==="ownerUpdates"} onClick={()=>setTab("ownerUpdates")} icon={Megaphone} label="Updates"/><NavButton active={tab==="ownerCalendar"} onClick={()=>setTab("ownerCalendar")} icon={Calendar} label="Calendar"/><NavButton active={tab==="ownerInvoices"} onClick={()=>setTab("ownerInvoices")} icon={CreditCard} label="Bills"/></nav>{tab==="ownerHome"&&<OwnerHome profile={profile} setTab={setTab}/>} {tab==="ownerHorses"&&<OwnerHorses profile={profile}/>} {tab==="ownerUpdates"&&<OwnerUpdates profile={profile}/>} {tab==="ownerCalendar"&&<OwnerCalendar profile={profile}/>} {tab==="ownerInvoices"&&<OwnerInvoices profile={profile}/>}</>}
function PhotoReel(){const[i,setI]=useState(0);useEffect(()=>{const t=setInterval(()=>setI(v=>(v+1)%photos.length),4500);return()=>clearInterval(t)},[]);return <div className="photo-reel" style={{backgroundImage:`url(${photos[i]})`}}/>}
function Login({mode,onBack}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[msg,setMsg]=useState("");async function submit(e){e.preventDefault();const{error}=await supabase.auth.signInWithPassword({email,password});if(error)setMsg(error.message)}return <main className="login-screen"><PhotoReel/><section className="login-card"><button className="text" onClick={onBack}>← Back</button><h1>{mode==="owner"?"Owners Portal":"Stable Login"}</h1><p>{mode==="owner"?"Owner-only access to horses, updates, invoices and calendar.":"Trainer/staff access to the stable operations side."}</p><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary full">Login</button></form>{msg&&<p className="error">{msg}</p>}</section></main>}
function InviteSignup({onBack,setToast}){const[code,setCode]=useState(""),[fullName,setFullName]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState("");async function submit(e){e.preventDefault();const{data:invite,error:ie}=await supabase.from("invite_codes").select("*").eq("code",code.trim()).is("used_by",null).single();if(ie||!invite){setToast("Invalid or used invite code.");return}const{data,error}=await supabase.auth.signUp({email,password});if(error){setToast(error.message);return}const uid=data.user?.id;if(!uid){setToast("Account created. Check email confirmation.");return}await supabase.from("profiles").insert({id:uid,stable_id:invite.stable_id,role:invite.role,full_name:fullName,owner_name:invite.owner_name||fullName});await supabase.from("invite_codes").update({used_by:uid,used_at:new Date().toISOString()}).eq("id",invite.id);setToast("Account created. Log in now.");onBack()}return <main className="login-screen"><PhotoReel/><section className="login-card"><button className="text" onClick={onBack}>← Back</button><h1>Join With Invite Code</h1><form onSubmit={submit}><label>Invite Code<input value={code} onChange={e=>setCode(e.target.value)}/></label><label>Full Name<input value={fullName} onChange={e=>setFullName(e.target.value)}/></label><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="primary full">Create Account</button></form></section></main>}
function Dashboard({stableId,setTab}){const[counts,setCounts]=useState({});useEffect(()=>{if(stableId)load()},[stableId]);async function count(t){const{count}=await supabase.from(t).select("*",{count:"exact",head:true}).eq("stable_id",stableId);return count||0}async function load(){setCounts({horses:await count("horses"),work:await count("work_entries"),owners:await count("owners"),races:await count("race_records"),invoices:await count("invoices")})}return <main className="page"><section className="hero-panel"><div><p className="eyebrow dark-text">Stable command centre</p><h2>Everything important, in one place.</h2></div></section><section className="stats"><Stat icon={BadgeCheck} label="Horses" value={counts.horses||0}/><Stat icon={ClipboardList} label="Work" value={counts.work||0}/><Stat icon={Users} label="Owners" value={counts.owners||0}/><Stat icon={Trophy} label="Races" value={counts.races||0}/><Stat icon={FileText} label="Invoices" value={counts.invoices||0}/></section><section className="quick-grid">{["horses","owners","work","racing","treatments","farrier","feed","gear","updates","analytics","invoices"].map(k=><button className="quick" key={k} onClick={()=>setTab(k)}>{k}<ChevronRight size={18}/></button>)}</section></main>}
function GenericTable({stableId,config,setToast}){const[rows,setRows]=useState([]),[horses,setHorses]=useState([]),[owners,setOwners]=useState([]),[search,setSearch]=useState(""),[modal,setModal]=useState(null),[calendar,setCalendar]=useState(false),[selectedDate,setSelectedDate]=useState("");useEffect(()=>{if(stableId){load();supabase.from("horses").select("name").eq("stable_id",stableId).then(({data})=>setHorses(data||[]));supabase.from("owners").select("name,email,phone").eq("stable_id",stableId).then(({data})=>setOwners(data||[]))}},[stableId,config.table]);async function load(){const{data,error}=await supabase.from(config.table).select("*").eq("stable_id",stableId).order("created_at",{ascending:false});if(error)setToast(error.message);setRows(data||[])}function blank(){const r={};config.fields.forEach(([k,_l,t,o])=>{if(t==="date")r[k]=new Date().toISOString().slice(0,10);else if(t==="number")r[k]="";else if(t==="select")r[k]=o[0];else if(t==="horseName")r[k]=horses[0]?.name||"";else if(t==="ownerName")r[k]=owners[0]?.name||"";else r[k]=""});return r}async function save(rec,mode){const clean={...rec,stable_id:stableId};config.fields.forEach(([k,_l,t])=>{if(t==="number")clean[k]=clean[k]===""?0:Number(clean[k])});const res=mode==="edit"?await supabase.from(config.table).update(clean).eq("id",clean.id).eq("stable_id",stableId).select().single():await supabase.from(config.table).insert(clean).select().single();if(res.error){setToast(res.error.message);return}await afterSave(config,res.data||clean,stableId);setToast("Saved");setModal(null);load()}async function del(id){await supabase.from(config.table).delete().eq("id",id).eq("stable_id",stableId);load()}const filtered=rows.filter(r=>JSON.stringify(r).toLowerCase().includes(search.toLowerCase()));const shown=calendar&&selectedDate&&config.calendarField?filtered.filter(r=>r[config.calendarField]===selectedDate):filtered;const Icon=config.icon;return <main className="page"><section className="module-header"><div className="module-icon"><Icon size={24}/></div><div><h2>{config.title}</h2></div></section><section className="toolbar"><div className="search-box"><Search size={18}/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={`Search ${config.title}`}/></div>{config.calendarField&&<button className="ghost" onClick={()=>setCalendar(!calendar)}><Calendar size={18}/>{calendar?"Card View":"Calendar"}</button>}<button className="primary" onClick={()=>setModal({mode:"add",record:blank()})}><Plus size={18}/>Add</button></section>{calendar&&config.calendarField&&<PhoneCalendar rows={rows} dateField={config.calendarField} selectedDate={selectedDate} setSelectedDate={setSelectedDate} title={config.title}/>}<section className="grid">{shown.map(row=><article className="record clickable" key={row.id} onClick={()=>config.profile?setModal({mode:"horseProfile",record:row}):config.ownerProfile?setModal({mode:"ownerProfile",record:row}):null}><div className="record-head"><div><h3>{row.name||row.horse_name||row.item_name||row.full_name||row.date||row.entry_date||"Record"}</h3><p>{row.status||row.category||row.role||""}</p></div><div className="button-row" onClick={e=>e.stopPropagation()}><button className="edit" onClick={()=>setModal({mode:"edit",record:{...row}})}><Edit3 size={16}/></button><button className="delete" onClick={()=>del(row.id)}><Trash2 size={16}/></button></div></div><div className="details">{config.display.map(k=><div key={k}><span>{labelize(k)}</span><strong>{formatValue(row[k])}</strong></div>)}</div></article>)}</section>{modal?.mode==="horseProfile"&&<HorseProfile horse={modal.record} stableId={stableId} onClose={()=>setModal(null)}/>} {modal?.mode==="ownerProfile"&&<OwnerProfile owner={modal.record} stableId={stableId} onClose={()=>setModal(null)}/>} {modal&&["add","edit"].includes(modal.mode)&&<RecordModal fields={config.fields} title={`${modal.mode==="edit"?"Edit":"Add"} ${config.title}`} record={modal.record} horses={horses} owners={owners} onClose={()=>setModal(null)} onSave={r=>save(r,modal.mode)}/>}</main>}
async function afterSave(config,record,stableId){if(["treatments","farrier_records"].includes(config.table)&&record.bill_to_owners==="Yes"&&Number(record.bill_amount)>0)await createOwnerInvoices(stableId,record.horse_name,Number(record.bill_amount),record.treatment_type||record.service_type||"Bill",config.table,record.id);if(config.table==="finance_entries"&&record.entry_type==="Expense"&&record.bill_to_owners==="Yes")await createOwnerInvoices(stableId,record.horse_name,Number(record.amount),record.category||"Expense",config.table,record.id);if(config.table==="updates")await distributeUpdate(stableId,record)}
async function createOwnerInvoices(stableId,horseName,amount,category,source_type,source_id){if(!horseName||!amount)return;const{data:shares}=await supabase.from("horse_owners").select("*").eq("stable_id",stableId).eq("horse_name",horseName);for(const s of shares||[]){const charge=Number((amount*Number(s.percentage||0)/100).toFixed(2));const{data:o}=await supabase.from("owners").select("*").eq("stable_id",stableId).eq("name",s.owner_name).maybeSingle();const line={id:crypto.randomUUID(),description:`${category} - ${horseName} (${s.percentage}%)`,quantity:1,unit_price:charge};await supabase.from("invoices").insert({stable_id:stableId,invoice_number:`INV-${new Date().toISOString().slice(0,10).replaceAll("-","")}-${String(Math.floor(Math.random()*9000)+1000)}`,client_name:s.owner_name,client_email:o?.email||"",client_phone:o?.phone||"",horse_name:horseName,due_date:new Date(Date.now()+14*86400000).toISOString().slice(0,10),status:"Draft",payment_status:"Unpaid",line_items:[line],amount:charge,gst:charge*GST_RATE,total:charge*(1+GST_RATE),source_type,source_id})}}
async function distributeUpdate(stableId,u){if(!u.horse_name)return;const{data:shares}=await supabase.from("horse_owners").select("owner_name").eq("stable_id",stableId).eq("horse_name",u.horse_name);for(const s of shares||[])await supabase.from("owner_notifications").insert({stable_id:stableId,owner_name:s.owner_name,horse_name:u.horse_name,title:u.title,body:u.body,notification_type:"Update"})}
function PhoneCalendar({rows,dateField,selectedDate,setSelectedDate,title}){const today=new Date(),[cursor,setCursor]=useState(new Date(today.getFullYear(),today.getMonth(),1)),[open,setOpen]=useState("");const y=cursor.getFullYear(),m=cursor.getMonth(),cells=[],start=(new Date(y,m,1).getDay()+6)%7,days=new Date(y,m+1,0).getDate();for(let i=0;i<start;i++)cells.push(null);for(let d=1;d<=days;d++)cells.push(new Date(y,m,d));while(cells.length%7)cells.push(null);const recs=iso=>rows.filter(r=>r[dateField]===iso);function pick(d){const iso=toIso(d);setSelectedDate(iso);setOpen(iso)}return <section className="phone-calendar-wrap"><div className="phone-calendar-shell"><div className="phone-calendar-top"><button className="calendar-round" onClick={()=>setCursor(new Date(y,m-1,1))}>‹</button><div><h3>{cursor.toLocaleString("en-AU",{month:"long",year:"numeric"})}</h3><p>{title} calendar</p></div><button className="calendar-round" onClick={()=>setCursor(new Date(y,m+1,1))}>›</button></div><div className="phone-calendar-actions"><button className="ghost" onClick={()=>pick(today)}>Today</button><button className="ghost" onClick={()=>{setSelectedDate("");setOpen("")}}>Show All</button></div><div className="phone-weekdays">{["M","T","W","T","F","S","S"].map((x,i)=><span key={x+i}>{x}</span>)}</div><div className="phone-calendar-grid">{cells.map((d,i)=>{const iso=d?toIso(d):"",c=d?recs(iso).length:0;return <button key={i} disabled={!d} className={`${selectedDate===iso?"selected":""} ${c?"has-records":""} ${iso===toIso(today)?"today":""}`} onClick={()=>d&&pick(d)}>{d&&<strong>{d.getDate()}</strong>}{c>0&&<small>{c}</small>}</button>})}</div></div><div className="calendar-day-panel"><h3>{open?formatDate(open):"Select a date"}</h3><div className="day-records">{open&&recs(open).map((r,i)=><article className="day-record" key={r.id||i}><strong>{r.horse_name||r.name||"Record"}</strong><span>{r.sector||r.status||r.treatment_type||r.service_type||r.category||""}</span></article>)}{open&&!recs(open).length&&<p className="empty">No records.</p>}</div></div></section>}
function UpdatesPanel({stableId,setToast}){const config={fields:[["title","Title","text"],["horse_name","Horse","horseName"],["category","Category","select",["Stable Update","Owner Update","Race Update","Vet Update","Work Update","General"]],["body","Update Message","textarea"],["photo_urls","Photo URLs","textarea"],["video_urls","Video URLs","textarea"],["link_urls","Links","textarea"],["visibility","Visibility","select",["internal","owners","public-preview"]],["send_status","Send Status","select",["Draft","Ready To Send","Sent"]]]};return <GenericTable stableId={stableId} config={{...config,table:"updates",title:"Updates",icon:Megaphone,display:["horse_name","category","visibility","send_status","body","photo_urls","video_urls","link_urls"]}} setToast={setToast}/>}
function Analytics({stableId}){const[data,setData]=useState({finance:[],work:[],races:[],horses:[]});useEffect(()=>{if(stableId)load()},[stableId]);async function load(){const[finance,work,races,horses]=await Promise.all([supabase.from("finance_entries").select("*").eq("stable_id",stableId),supabase.from("work_entries").select("*").eq("stable_id",stableId),supabase.from("race_records").select("*").eq("stable_id",stableId),supabase.from("horses").select("*").eq("stable_id",stableId)]);setData({finance:finance.data||[],work:work.data||[],races:races.data||[],horses:horses.data||[]})}const completed=data.races.filter(r=>r.status==="Completed"),pm=completed.reduce((s,r)=>s+Number(r.prizemoney||0),0),income=data.finance.filter(f=>f.entry_type==="Income").reduce((s,f)=>s+Number(f.amount||0),0)+pm,expenses=data.finance.filter(f=>f.entry_type==="Expense").reduce((s,f)=>s+Number(f.amount||0),0),wins=completed.filter(r=>String(r.result).toLowerCase().includes("1")||String(r.result).toLowerCase().includes("win")).length,placings=completed.filter(r=>["1","1st","2","2nd","3","3rd"].includes(String(r.result).toLowerCase())).length;return <main className="page"><section className="module-header"><div className="module-icon"><BarChart3 size={24}/></div><div><h2>Analytics</h2></div></section><section className="stats"><Stat icon={DollarSign} label="Income" value={`$${income.toLocaleString()}`}/><Stat icon={DollarSign} label="Expenses" value={`$${expenses.toLocaleString()}`}/><Stat icon={BarChart3} label="Net" value={`$${(income-expenses).toLocaleString()}`}/><Stat icon={ClipboardList} label="Work" value={data.work.length}/><Stat icon={Trophy} label="Starts" value={completed.length}/><Stat icon={Trophy} label="Wins" value={wins}/><Stat icon={Trophy} label="Placings" value={placings}/><Stat icon={DollarSign} label="Prizemoney" value={`$${pm.toLocaleString()}`}/></section></main>}
function HorseProfile({horse,stableId,onClose}){const[data,setData]=useState({});useEffect(()=>{load()},[horse.name]);async function load(){const tables=[["work","work_entries","date"],["races","race_records","date"],["vet","treatments","treatment_date"],["farrier","farrier_records","farrier_date"],["feed","feed_programs","created_at"],["gear","gear_items","created_at"],["owners","horse_owners","created_at"],["invoices","invoices","created_at"]];const out={};for(const[t,table,order]of tables){const{data}=await supabase.from(table).select("*").eq("stable_id",stableId).eq("horse_name",horse.name).order(order,{ascending:false});out[t]=data||[]}setData(out)}async function addOwner(){const owner_name=prompt("Owner name");const percentage=prompt("Percentage");if(owner_name&&percentage){await supabase.from("horse_owners").insert({stable_id:stableId,horse_name:horse.name,owner_name,percentage:Number(percentage)});load()}}async function delShare(id){await supabase.from("horse_owners").delete().eq("id",id).eq("stable_id",stableId);load()}return <div className="modal-backdrop"><section className="modal profile-modal"><div className="modal-head"><h2>{horse.name}</h2><button onClick={onClose}><X size={20}/></button></div><button className="primary" onClick={addOwner}>Add Owner Share</button><ProfileSection title="owners">{(data.owners||[]).map((r,i)=><div className="profile-row" key={r.id||i}><span>{r.owner_name}</span><strong>{r.percentage}%</strong><button className="delete small" onClick={()=>delShare(r.id)}><Trash2 size={14}/></button></div>)}</ProfileSection>{["work","races","vet","farrier","feed","gear","invoices"].map(k=><ProfileSection key={k} title={k}>{(data[k]||[]).map((r,i)=><div className="profile-row" key={r.id||i}><span>{r.date||r.treatment_date||r.farrier_date||r.item_name||r.invoice_number||r.sector||r.status||r.service_type||r.treatment_type||r.horse_name}</span><strong>{r.total?`$${r.total}`:r.prizemoney?`$${r.prizemoney}`:""}</strong></div>)}</ProfileSection>)}</section></div>}
function OwnerProfile({owner,stableId,onClose}){const[shares,setShares]=useState([]),[invoices,setInvoices]=useState([]),[updates,setUpdates]=useState([]);useEffect(()=>{supabase.from("horse_owners").select("*").eq("stable_id",stableId).eq("owner_name",owner.name).then(({data})=>{setShares(data||[]);const names=(data||[]).map(s=>s.horse_name);if(names.length)supabase.from("updates").select("*").eq("stable_id",stableId).in("horse_name",names).then(({data})=>setUpdates(data||[]))});supabase.from("invoices").select("*").eq("stable_id",stableId).eq("client_name",owner.name).then(({data})=>setInvoices(data||[]))},[owner.name]);return <div className="modal-backdrop"><section className="modal"><div className="modal-head"><h2>{owner.name}</h2><button onClick={onClose}><X size={20}/></button></div><ProfileSection title="Horses Owned">{shares.map(s=><div className="profile-row" key={s.id}><span>{s.horse_name}</span><strong>{s.percentage}%</strong></div>)}</ProfileSection><ProfileSection title="Invoices">{invoices.map(i=><div className="profile-row" key={i.id}><span>{i.invoice_number}</span><strong>${i.total}</strong></div>)}</ProfileSection><ProfileSection title="Updates Received">{updates.map(u=><div className="profile-row" key={u.id}><span>{u.title}</span><strong>{u.horse_name}</strong></div>)}</ProfileSection></section></div>}
function OwnerHome({profile,setTab}){return <main className="page"><section className="hero-panel"><div><p className="eyebrow dark-text">Owners Portal</p><h2>Welcome, {profile.owner_name||profile.full_name}</h2></div></section><section className="quick-grid">{[["Horses","ownerHorses"],["Updates","ownerUpdates"],["Calendar","ownerCalendar"],["Bills","ownerInvoices"]].map(([a,b])=><button className="quick" onClick={()=>setTab(b)} key={b}>{a}<ChevronRight size={18}/></button>)}</section></main>}
function useOwnerData(profile){const[state,setState]=useState({horses:[],updates:[],invoices:[],events:[]});useEffect(()=>{if(profile?.stable_id)load()},[profile?.stable_id]);async function load(){const{data:shares}=await supabase.from("horse_owners").select("*").eq("stable_id",profile.stable_id).eq("owner_name",profile.owner_name);const names=(shares||[]).map(s=>s.horse_name);const[horses,updates,invoices,races,work,vet,farrier]=await Promise.all([names.length?supabase.from("horses").select("*").eq("stable_id",profile.stable_id).in("name",names):{data:[]},names.length?supabase.from("updates").select("*").eq("stable_id",profile.stable_id).in("horse_name",names).in("visibility",["owners","public-preview"]).order("created_at",{ascending:false}):{data:[]},supabase.from("invoices").select("*").eq("stable_id",profile.stable_id).eq("client_name",profile.owner_name),names.length?supabase.from("race_records").select("*").eq("stable_id",profile.stable_id).in("horse_name",names):{data:[]},names.length?supabase.from("work_entries").select("*").eq("stable_id",profile.stable_id).in("horse_name",names):{data:[]},names.length?supabase.from("treatments").select("*").eq("stable_id",profile.stable_id).in("horse_name",names):{data:[]},names.length?supabase.from("farrier_records").select("*").eq("stable_id",profile.stable_id).in("horse_name",names):{data:[]}]);setState({horses:(horses.data||[]).map(h=>({...h,percentage:(shares||[]).find(s=>s.horse_name===h.name)?.percentage||0})),updates:updates.data||[],invoices:invoices.data||[],events:[...(races.data||[]).map(x=>({...x,date:x.date,type:"Race"})),...(work.data||[]).map(x=>({...x,date:x.date,type:"Work"})),...(vet.data||[]).map(x=>({...x,date:x.treatment_date,type:"Vet"})),...(farrier.data||[]).map(x=>({...x,date:x.farrier_date,type:"Farrier"}))]})}return state}
function OwnerHorses({profile}){const{horses}=useOwnerData(profile);return <main className="page"><section className="grid">{horses.map(h=><article className="record" key={h.name}><h3>{h.name}</h3><p>{h.percentage}% owned</p></article>)}</section></main>}
function OwnerUpdates({profile}){const{updates}=useOwnerData(profile);return <main className="page"><section className="grid">{updates.map(u=><article className="record" key={u.id}><h3>{u.title}</h3><p>{u.body}</p><UpdateMediaPreview u={u}/></article>)}</section></main>}
function OwnerCalendar({profile}){const{events}=useOwnerData(profile);return <main className="page"><PhoneCalendar rows={events} dateField="date" selectedDate="" setSelectedDate={()=>{}} title="Owner"/></main>}
function OwnerInvoices({profile}){const{invoices}=useOwnerData(profile);return <main className="page"><section className="grid">{invoices.map(i=><article className="record" key={i.id}><h3>{i.invoice_number}</h3><p>{i.horse_name} · ${i.total}</p></article>)}</section></main>}
function UpdateMediaPreview({u}){return <div className="media-preview">{splitLines(u.photo_urls).map(x=><a href={x} target="_blank"><img src={x}/></a>)}{splitLines(u.video_urls).map((x,i)=><a className="media-pill" href={x} target="_blank">Video {i+1}</a>)}{splitLines(u.link_urls).map((x,i)=><a className="media-pill" href={x} target="_blank">Link {i+1}</a>)}</div>}
function RecordModal({fields,record,horses,owners,onClose,onSave,title}){const[form,setForm]=useState(record);return <div className="modal-backdrop"><section className="modal"><div className="modal-head"><h2>{title}</h2><button onClick={onClose}><X size={20}/></button></div><div className="form-grid">{fields.filter(([_k,_l,t])=>t!=="conditionalWarmup"||WARMUP_SECTORS.includes(form.sector)).map(([k,l,t,o])=><Field key={k} label={l}><Input type={t} value={form[k]??""} options={o} horses={horses} owners={owners} onChange={v=>setForm(c=>({...c,[k]:v}))}/></Field>)}</div><button className="primary full" onClick={()=>onSave(form)}>Save</button></section></div>}
function Invoices({stableId,setToast}){const[rows,setRows]=useState([]),[horses,setHorses]=useState([]),[modal,setModal]=useState(null),[printInvoice,setPrintInvoice]=useState(null),[shareModal,setShareModal]=useState(null);useEffect(()=>{if(stableId)load()},[stableId]);async function load(){const{data,error}=await supabase.from("invoices").select("*").eq("stable_id",stableId).order("created_at",{ascending:false});if(error)setToast(error.message);setRows(data||[]);const h=await supabase.from("horses").select("name").eq("stable_id",stableId);setHorses(h.data||[])}function blank(){return{invoice_number:`INV-${new Date().toISOString().slice(0,10).replaceAll("-","")}-${String(rows.length+1).padStart(3,"0")}`,client_name:"",client_email:"",client_phone:"",horse_name:horses[0]?.name||"",due_date:new Date(Date.now()+14*86400000).toISOString().slice(0,10),status:"Draft",payment_status:"Unpaid",notes:"",line_items:[{id:crypto.randomUUID(),description:"",quantity:1,unit_price:0}]}}async function save(inv,mode){const totals=invoiceTotals(inv);const clean={...inv,stable_id:stableId,amount:totals.subtotal,gst:totals.gst,total:totals.total};const res=mode==="edit"?await supabase.from("invoices").update(clean).eq("id",clean.id).eq("stable_id",stableId):await supabase.from("invoices").insert(clean);if(res.error)setToast(res.error.message);else{setToast("Saved");setModal(null);load()}}async function remove(id){await supabase.from("invoices").delete().eq("id",id).eq("stable_id",stableId);load()}return <main className="page"><section className="module-header"><div className="module-icon"><FileText size={24}/></div><div><h2>Invoices</h2></div></section><section className="toolbar"><button className="primary" onClick={()=>setModal({mode:"add",invoice:blank()})}><Plus size={18}/>Create Invoice</button></section><section className="grid">{rows.map(inv=>{const totals=invoiceTotals(inv);return <article className="record" key={inv.id}><div className="record-head"><div><h3>{inv.invoice_number}</h3><p>{inv.client_name} · {inv.horse_name}</p></div><div className="button-row"><button className="edit" onClick={()=>setModal({mode:"edit",invoice:inv})}><Edit3 size={16}/></button><button className="print" onClick={()=>setPrintInvoice(inv)}><Printer size={16}/></button><button className="share" onClick={()=>setShareModal(inv)}><Share2 size={16}/></button><button className="delete" onClick={()=>remove(inv.id)}><Trash2 size={16}/></button></div></div><div className="details"><div><span>Status</span><strong>{inv.payment_status||inv.status}</strong></div><div><span>Due</span><strong>{inv.due_date}</strong></div><div><span>Total</span><strong>${totals.total.toFixed(2)}</strong></div></div></article>})}</section>{modal&&<InvoiceModal modal={modal} horses={horses} onClose={()=>setModal(null)} onSave={save}/>} {shareModal&&<ShareInvoice invoice={shareModal} onClose={()=>setShareModal(null)} setToast={setToast}/>} {printInvoice&&<InvoicePrint invoice={printInvoice} onClose={()=>setPrintInvoice(null)}/>}</main>}
function InvoiceModal({modal,horses,onClose,onSave}){const[inv,setInv]=useState(modal.invoice);const totals=invoiceTotals(inv);function setField(k,v){setInv(c=>({...c,[k]:v}))}function setLine(id,k,v){setInv(c=>({...c,line_items:(c.line_items||[]).map(li=>li.id===id?{...li,[k]:v}:li)}))}function addLine(){setInv(c=>({...c,line_items:[...(c.line_items||[]),{id:crypto.randomUUID(),description:"",quantity:1,unit_price:0}]}))}function removeLine(id){setInv(c=>({...c,line_items:(c.line_items||[]).filter(li=>li.id!==id)}))}return <div className="modal-backdrop"><section className="modal invoice-modal"><div className="modal-head"><h2>{modal.mode==="edit"?"Edit":"Create"} Invoice</h2><button onClick={onClose}><X size={20}/></button></div><div className="form-grid"><Field label="Invoice Number"><input value={inv.invoice_number||""} onChange={e=>setField("invoice_number",e.target.value)}/></Field><Field label="Client Name"><input value={inv.client_name||""} onChange={e=>setField("client_name",e.target.value)}/></Field><Field label="Client Email"><input value={inv.client_email||""} onChange={e=>setField("client_email",e.target.value)}/></Field><Field label="Client Phone"><input value={inv.client_phone||""} onChange={e=>setField("client_phone",e.target.value)}/></Field><Field label="Horse"><select value={inv.horse_name||""} onChange={e=>setField("horse_name",e.target.value)}>{horses.map(h=><option key={h.name}>{h.name}</option>)}</select></Field><Field label="Due Date"><input type="date" value={inv.due_date||""} onChange={e=>setField("due_date",e.target.value)}/></Field><Field label="Status"><select value={inv.status||"Draft"} onChange={e=>setField("status",e.target.value)}><option>Draft</option><option>Sent</option><option>Part Paid</option><option>Paid</option><option>Overdue</option></select></Field><Field label="Payment Status"><select value={inv.payment_status||"Unpaid"} onChange={e=>setField("payment_status",e.target.value)}><option>Unpaid</option><option>Part Paid</option><option>Paid</option></select></Field><Field label="Notes"><textarea value={inv.notes||""} onChange={e=>setField("notes",e.target.value)}/></Field></div><section className="invoice-lines"><div className="section-title"><h3>Line Items</h3><button className="text" onClick={addLine}>+ Add Line</button></div>{(inv.line_items||[]).map(li=><div className="line-item" key={li.id}><input placeholder="Description" value={li.description||""} onChange={e=>setLine(li.id,"description",e.target.value)}/><input type="number" placeholder="Qty" value={li.quantity||0} onChange={e=>setLine(li.id,"quantity",e.target.value)}/><input type="number" placeholder="Unit $" value={li.unit_price||0} onChange={e=>setLine(li.id,"unit_price",e.target.value)}/><strong>${(Number(li.quantity||0)*Number(li.unit_price||0)).toFixed(2)}</strong><button className="delete small" onClick={()=>removeLine(li.id)}><Trash2 size={14}/></button></div>)}</section><section className="invoice-totals"><div><span>Subtotal</span><strong>${totals.subtotal.toFixed(2)}</strong></div><div><span>GST</span><strong>${totals.gst.toFixed(2)}</strong></div><div><span>Total</span><strong>${totals.total.toFixed(2)}</strong></div></section><button className="primary full" onClick={()=>onSave(inv,modal.mode)}>Save Invoice</button></section></div>}
function ShareInvoice({invoice,onClose,setToast}){const totals=invoiceTotals(invoice);const message=`Hi ${invoice.client_name||""},\n\nPlease find invoice ${invoice.invoice_number} for ${invoice.horse_name}.\n\nTotal: $${totals.total.toFixed(2)}\nDue: ${invoice.due_date}`;async function phoneShare(){if(navigator.share)await navigator.share({title:`Invoice ${invoice.invoice_number}`,text:message});else{await navigator.clipboard.writeText(message);setToast("Invoice copied")}}async function email(){const res=await fetch("/api/send-email",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({to:invoice.client_email,subject:`Invoice ${invoice.invoice_number}`,message})});setToast(res.ok?"Email sent/demo sent":"Email failed");onClose()}return <div className="modal-backdrop"><section className="modal"><div className="modal-head"><h2>Share Invoice</h2><button onClick={onClose}><X size={20}/></button></div><label className="field"><span>Message</span><textarea value={message} readOnly/></label><div className="button-row wide"><button className="primary" onClick={phoneShare}><Share2 size={18}/>Phone Share Sheet</button><button className="primary" onClick={email}><Mail size={18}/>Send Email / Demo API</button></div></section></div>}
function InvoicePrint({invoice,onClose}){const totals=invoiceTotals(invoice);return <div className="print-backdrop"><section className="print-actions"><button className="primary" onClick={()=>window.print()}><Printer size={18}/>Print / Save PDF</button><button className="danger" onClick={onClose}>Close</button></section><main className="invoice-print"><header><h1>Invoice</h1><strong>{invoice.invoice_number}</strong></header><p>Bill To: {invoice.client_name}</p><p>Horse: {invoice.horse_name}</p><table><thead><tr><th>Description</th><th>Qty</th><th>Unit</th><th>Total</th></tr></thead><tbody>{(invoice.line_items||[]).map(li=><tr key={li.id}><td>{li.description}</td><td>{li.quantity}</td><td>${Number(li.unit_price||0).toFixed(2)}</td><td>${(Number(li.quantity||0)*Number(li.unit_price||0)).toFixed(2)}</td></tr>)}</tbody></table><h2>Total: ${totals.total.toFixed(2)}</h2></main></div>}
function ProfileSection({title,children}){return <section className="profile-section"><h3>{title}</h3>{children}</section>}
function Input({type,value,options,horses,owners,onChange}){if(type==="textarea"||type==="conditionalWarmup")return <textarea value={value} onChange={e=>onChange(e.target.value)}/>;if(type==="select")return <select value={value} onChange={e=>onChange(e.target.value)}>{options.map(o=><option key={o}>{o}</option>)}</select>;if(type==="horseName")return <select value={value} onChange={e=>onChange(e.target.value)}>{horses.map(h=><option key={h.name}>{h.name}</option>)}</select>;if(type==="ownerName")return <select value={value} onChange={e=>onChange(e.target.value)}>{owners.map(o=><option key={o.name}>{o.name}</option>)}</select>;return <input type={type} value={value} onChange={e=>onChange(e.target.value)}/>}
function Field({label,children}){return <label className="field"><span>{label}</span>{children}</label>}function NavButton({active,onClick,icon:Icon,label}){return <button className={active?"active":""} onClick={onClick}><Icon size={15}/>{label}</button>}function Stat({icon:Icon,label,value}){return <section className="stat"><div><Icon size={22}/></div><p>{label}</p><strong>{value}</strong></section>}function toIso(d){return`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,"0")}-${String(d.getDate()).padStart(2,"0")}`}function formatDate(v){if(!v)return"-";const d=new Date(v);return Number.isNaN(d.getTime())?String(v):d.toLocaleDateString("en-AU",{day:"numeric",month:"short",year:"numeric"})}function splitLines(v){return String(v||"").split(/\n|,/).map(x=>x.trim()).filter(Boolean)}function labelize(v){return v.replace(/_/g," ").replace(/\b\w/g,l=>l.toUpperCase())}function formatValue(v){return v===null||v===undefined||v===""?"-":String(v)}function invoiceTotals(inv){const subtotal=(inv.line_items||[]).reduce((s,li)=>s+Number(li.quantity||0)*Number(li.unit_price||0),0),gst=subtotal*GST_RATE;return{subtotal,gst,total:subtotal+gst}}
createRoot(document.getElementById("root")).render(<App/>);
