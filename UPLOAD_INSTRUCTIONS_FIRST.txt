SEPARATE 2FA COMPONENTS PATCH

This patch does NOT touch package.json, package-lock.json, styles.css, or SQL.

Upload/copy only these files:
1. src/main.jsx
2. src/components/MfaSettings.jsx
3. src/components/MfaChallenge.jsx

Commit message:
Add 2FA as separate components

Supabase requirement:
Authentication > Multi-Factor > TOTP must be enabled.

Rollback:
If anything goes wrong, revert only this commit.

Notes:
- Existing remembered sessions should not be interrupted every morning.
- Verified 2FA users are challenged on fresh login/new browser/new device when Supabase requires aal2.
- I did not run a full npm build here because the build step itself is what has been hanging.
