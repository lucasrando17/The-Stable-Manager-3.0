SECURITY SETTINGS / 2FA SETUP PAGE PATCH

This is deliberately NOT the full MFA login challenge yet.

It does:
- Adds src/pages/security/SecuritySettings.jsx
- Replaces the Security card with that component
- Allows users to enroll and verify authenticator-app 2FA
- Allows disabling/clearing 2FA factors

It does NOT:
- Touch package.json
- Touch package-lock.json
- Touch styles.css
- Change the core login/auth flow
- Force morning login prompts

Upload/copy only:
1. src/main.jsx
2. src/pages/security/SecuritySettings.jsx

Commit message:
Add separate security settings component

If this deploys quickly and works, the next patch is the separate login challenge.
